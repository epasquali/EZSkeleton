//This file contains the form headers for common pages that use the FormBox
import { APPNAME } from "./metadata";

export const AUTH_HEADERS = {
  login: ["Log In to access your Dashboard", "🌟"],
  register: [`Welcome to ${APPNAME}! ☺️`, "Create your account below."],
};
